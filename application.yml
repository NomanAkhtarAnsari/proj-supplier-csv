spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/supplier_dashboard  #supplier_dashboard is the database name
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  liquibase:
    changelog: classpath:/db/changelog/db.changelog-master.xml